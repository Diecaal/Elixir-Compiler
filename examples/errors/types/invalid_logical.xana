def always_true() :: bool do
    return true
end

def main() do
    if always_true() do
        puts 't', 'r', 'u', 'e', '\n'
    end

    if false do # Error
    end

    aux :: bool
    aux = false

    while !aux do
        aux = true
    end

    condition :: bool
    condition = !false
    condition = true && false
    condition = false || true
end
