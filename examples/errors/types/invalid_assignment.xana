def main() do
    id :: int
    letter :: char
    real :: double

    id = 1
    real = 2
    real = id # real var is set to int type?
    id = 10.2 # Error
    letter = id # Error
    letter = 12.2 # Error
    real = letter
end
